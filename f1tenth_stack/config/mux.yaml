ackermann_mux:
  ros__parameters:
    topics:
      navigation:
        topic   : drive
        timeout : 0.2
        priority: 10
      joystick:
        topic   : teleop
        timeout : 0.2
        priority: 100
    # locks:
    #   nav_control:
    #     topic: nav_control  
    #     timeout: 0.2
    #     priority: 50`